import { 
  useListPrograms, 
  useGetProgram, 
  useSubmitContact, 
  useListTestimonials 
} from "@workspace/api-client-react";

// Re-exporting generated hooks to maintain clean component imports
// and adhere to project structural requirements.
export {
  useListPrograms,
  useGetProgram,
  useSubmitContact,
  useListTestimonials
};
